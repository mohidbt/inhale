async def retrieve_chunks(document_id: int, query: str, top_k: int = 5) -> list[dict]:
    """
    Retrieve relevant document chunks via pgvector similarity search.
    Full implementation in Phase 1.1 when embeddings are stored.
    Returns empty list until then.
    """
    return []
